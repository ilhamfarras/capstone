langkah yang dilakukan sebelum memulai aplikasi

pertama isnstall
  pip install -U Flask-SQLAlchemy

kedua indtall
  pip install -r requirements.txt

ketiga set enviornment variablels
  set FLASK_APP=application.py

terakhir jalankan aplikasi
  flask run
